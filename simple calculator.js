// 
function getTotal(a, b) {
    return a + b
}
var num1 = 333332;
var num2 = 777773;
const total = getTotal(num1, num2);
console.log('The sum total is' + total);


//
function getDistance(mph, h) {
    return mph * h
}
var mph = 60;
var h = 2;
distance = getDistance(mph, h);
console.log(`The distance is ${distance}`);

//converting kelvin to Celcious the Feheriheit
